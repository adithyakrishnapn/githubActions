name: My First Workflow

on:
  push:
    branches: [ master ]

jobs:
  test-site:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout Code
       uses: actions/checkout@v4
     - name: Print Success Message
       run: echo "Website pipeline working!"